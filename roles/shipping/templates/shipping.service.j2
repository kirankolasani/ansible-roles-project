[Unit]
Description=Shipping Service

[Service]
User=roboshop
Environment=CART_ENDPOINT="{{hostvars['cart'].ansible_host}}:8080"
Environment=DB_HOST="{{hostvars['mysql'].ansible_host}}"
ExecStart=/bin/java -jar /app/shipping.jar
SyslogIdentifier=shipping

[Install]
WantedBy=multi-user.target